{
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public --project-name=dynamic-import-cold-start-pages --branch=large"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240222.0",
    "wrangler": "^3.38.0"
  }
}
